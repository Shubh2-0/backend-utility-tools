# Connection pooling with HikariCP

_Generated 2026-06-02_

95% of Java developers are using connection pooling wrong, and it's killing their application's performance. I've seen it happen in production me, where a simple misconfiguration can bring down the entire system. When it comes to connection pooling with HikariCP, the key is to find the sweet spot between connection creation and reuse. Too few connections, and you'll be waiting for database access; too many, and you'll overwhelm your database. 
```java
HikariConfig config = new HikariConfig();
config.setJdbcUrl("jdbc:mysql://localhost:3306/mydb");
config.setUsername("user");
config.setPassword("password");
config.setMinimumIdle(5);
config.setMaximumPoolSize(10);
```
In my experience, setting the minimum idle connections to 5 and maximum pool size to 10 works well for most use cases, scale pe. But what works for your application? Are you using HikariCP with default settings, or have you customized it to fit your specific needs - what's your strategy for optimizing connection pooling in production?

---

*By Shubham Bhati — Backend Engineer at AlignBits LLC. [Portfolio](https://shubh2-0.github.io) · [LinkedIn](https://linkedin.com/in/bhatishubham) · [GitHub](https://github.com/Shubh2-0)*
